[build]
  command = "npm run install:all && node scripts/prepare-firebase-config.mjs && npm run build"
  publish = "frontend/dist"

[build.environment]
  VITE_USE_FIREBASE_EMULATOR = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
